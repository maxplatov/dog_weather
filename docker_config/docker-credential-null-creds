#!/bin/sh
# Fake credential helper — returns empty credentials for headless servers
case "$1" in
  list)  echo '{}';;
  get)   echo '{"ServerURL":"","Username":"","Secret":""}';;
  store|erase) ;;
esac
exit 0
